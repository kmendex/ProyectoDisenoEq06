class Direccion {
    constructor(provincia, canton, districto, info) {
      this.provincia = provincia;
      this.canton = canton;
      this.districto = districto;
      this.info = info;
    }
}

